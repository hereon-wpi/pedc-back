/**
 * Do not expose this key publicly.
 * You must protect this key using appropriate measures such as
 * a secrets vault, environment variable, or configuration service.
 */

export const jwtConstants = {
  secret: 'secretKey',
};
